interface:
  display_name: "whiplash-loop"
  short_description: "Evidence-driven chat retry loop for 위플래쉬 or 플레처소환 code tasks"
  default_prompt: "Use whiplash-loop when the user explicitly wants a stronger proof-first review and retry loop for a code-change or code-review task."
policy:
  allow_implicit_invocation: true
