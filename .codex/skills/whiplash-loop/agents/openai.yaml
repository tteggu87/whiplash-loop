interface:
  display_name: "whiplash-loop"
  short_description: "Proof-first retry loop with reviewer and 3-worker swarm"
  default_prompt: "Use $whiplash-loop to run a proof-first Fletcher retry loop with a separated reviewer and low/medium/high workers on this code task."
policy:
  allow_implicit_invocation: true
